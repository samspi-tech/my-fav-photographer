import PageNotFound from '../components/pageNotFound/PageNotFound.jsx';

const NotFoundPage = () => {
    return (
        <>
            <PageNotFound />
        </>
    );
};

export default NotFoundPage;
